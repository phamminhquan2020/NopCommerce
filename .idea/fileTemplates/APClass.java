package ${PACKAGE_NAME};

import commonFunctions.AbstractPage;
import org.openqa.selenium.WebDriver;

public class ${NAME} extends AbstractPage {
    private WebDriver driver;

    public ${NAME}(WebDriver driver) {
        this.driver = driver;
    }
{cursor}
}
